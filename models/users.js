

/*

const UserSchema = new Schema({
  name: {
    type: String,
    required: true
  },
  email: {
    type: String,
    required: true
  },
  password: {
    type: String,
    required: true
  }
});

const Users = mongoose.model('Users', UserSchema); */











/*
array.users.forEach(function(obj){
    Users.findOne(obj).then(existeUser => {
      if(!existeUser){
        let user = new Users(obj);
        user.save();
      }
    }).catch(err => {
      throw err;
    });
  }); */
  